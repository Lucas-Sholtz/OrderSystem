@model IEnumerable<OrdersSystem.Town>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TownName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TownPostCode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TownName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TownPostCode)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TownId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TownId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TownId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
